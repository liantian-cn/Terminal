# Terminal 文档入口

这套文档现在位于 `Terminal/.context/`，是 `Terminal` 这一侧的唯一正式文档入口。

## 建议阅读顺序

1. `00_project_overview.md`
2. `01_shared_protocol.md`
3. `03_color_conventions.md`
4. `20_terminal_scope.md`
5. `21_terminal_architecture.md`
6. `22_terminal_dev_rules.md`

## 当前定位

`Terminal` 现在是架构阶段，不提前锁死所有实现细节。

本目录优先回答:

- 它要解决什么问题
- 它和 `DejaVu` 的关系是什么
- 它应该分成哪些层
- 开发时什么能做，什么不能做
